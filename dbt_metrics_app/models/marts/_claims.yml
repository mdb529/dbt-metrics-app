version: 2

sources:
    - name: raw
      description: Raw source dummy claims data
      database: dbt-metrics-dw

      tables:
        - name: raw_claim_lines
        - name: raw_medical_groups
        - name: raw_users
        - name: raw_patients
        - name: raw_medical_groups_users
        - name: raw_patient_access_log
